// mapMain.js
import { locationPair } from './locationPairClass.js';

/*
import { createSvg } from './mapPlateCarree.js';

window.addEventListener('DOMContentLoaded', () => {
    document.getElementById('make-maps-button').addEventListener('click', () => {
        
        const locationPairs = locationPair.locationPairs;
        // Log the arrays to the console
        console.log(locationPairs);

        // Call the function to create the SVG
        createSvg();
    });
});
*/